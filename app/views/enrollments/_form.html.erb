<%= form_for(@enrollment) do |f| %>
  <% if @enrollment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@enrollment.errors.count, "error") %> prohibited this enrollment from being saved:</h2>

      <ul>
      <% @enrollment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label "Materia" %><br>
    <%= select_tag "subject", options_from_collection_for_select(Subject.all, 'id', 'name') %>
    <%#= f.hidden_field "student_for_enrollment", :value => @enrollment.student.id%>
    <%= hidden_field_tag :student_for_enrollment, @enrollment.student.id %>
  </div>

  <div class="field">
    <%= f.label "Cátedra" %><br>
    <%= f.text_field :professorship %>
  </div>

  <div class="field">
    <%= f.label "Turno" %><br>
    <%= f.text_field :shift %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
