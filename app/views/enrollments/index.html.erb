<p id="notice"><%= notice %></p>

<!-- TODO need to use the parameter to write the Student's name here, probably need another template -->
<% if false %>
<h1>Listing Enrollments<!-- <%#for <%= @enrollments.first.student.last_name %>, <%= @enrollments.first.student.first_name %>--></h1>
<% end %>
<h1>Listing Enrollments <%= get_student_full_name(@student) %></h1>

<table>
  <thead>
    <tr>
      <th>Year</th>
      <th>Subject Name</th>
      <!-- <th>Career</th> -->    <!-- TODO if this is necessary, deal with a nil Career for a Subject -->
      <th>Professorship</th>
      <th>Shift</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @enrollments.each do |enrollment| %>
      <tr>
        <td><%= enrollment.year %></td>
        <td><%= enrollment.subject.name %></td>
        <td><%# <%= enrollment.subject.career.name %></td>
        <td><%= enrollment.professorship %></td>
        <td><%= enrollment.shift %></td>
        <td><%= link_to 'Destroy', enrollment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%#= link_to 'New Enrollment', new_enrollment_path %>
<%= link_to "New Enrollment", new_enrollment_path(:student => @student.id) %>
