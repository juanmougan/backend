<%= form_for(@subscription_list) do |f| %>
  <% if @subscription_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subscription_list.errors.count, "error") %> prohibited this subscription_list from being saved:</h2>

      <ul>
      <% @subscription_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Nombre" %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "Descripción" %><br>
    <%= f.text_area :description %>
  </div>
  <div id="field" style="width: 50%;">
    <div id="left" style="float: left; width: 50%;"> 
        <%= label_tag(:rule_career, "Por carrera") %>
        <%= radio_button_tag(:rule, "career") %><br>
        <!-- <%#= label_tag(:rule_year, "By year") %> -->
        <!-- <%#= radio_button_tag(:rule, "year") %><br> -->
        <%= label_tag(:rule_subject, "Por materia") %>
        <%= radio_button_tag(:rule, "subject") %><br>
    </div>
    <div id="right" style="float: left; width: 50%;">
      <!-- TODO shouldn't be hardcoded -->
      <!--
      <select>
        <option value="civil">Civil</option>
        <option value="industrial">Industrial</option>
        <option value="informatica">Informática</option>
        <option value="ambiental">Ambiental</option>
      </select>
    -->
    <% if false %>
      <%= select_tag "careers", options_from_collection_for_select(Career.all, 'id', 'name', @student.subscription_lists.map { |j| j.id }) %>
    <% end %>
      <%= select_tag "careers", options_from_collection_for_select(Career.all, 'id', 'name') %>
      <br>
      
      <!-- TODO re-add this when some information about a subject's year becomes available -->
      <!-- 
      <select>
        <option value="first">First</option>
        <option value="second">Second</option>
        <option value="third">Third</option>
        <option value="fourth">Fourth</option>
        <option value="fifth">Fifth</option>
      </select>
      <br>
    -->

      <!-- TODO shouldn't be hardcoded -->
      <!--
      <select>
        <option value="algebra">Álgebra</option>
        <option value="calculo">Cálculo</option>
        <option value="fisica">Física</option>
        <option value="redes">Redes</option>
      </select>
      -->
        <%= select_tag "subjects", options_from_collection_for_select(Subject.all, 'id', 'name_with_code') %>

      <br>
    </div>
    <br><br><br><br>
</div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
